package cn.fastposter.cloud.client;import cn.fastposter.cloud.client.utils.Base64Utils;import cn.fastposter.cloud.client.utils.DigestUtils;import cn.fastposter.cloud.client.utils.RandomUtils;import com.fasterxml.jackson.annotation.JsonIgnoreProperties;import com.sun.org.apache.xalan.internal.xsltc.compiler.Stylesheet;import jdk.nashorn.internal.runtime.Debug;import lombok.Builder;import lombok.Data;import lombok.SneakyThrows;import cn.fastposter.cloud.client.utils.JsonUtils;import lombok.extern.slf4j.Slf4j;import okhttp3.*;import java.text.SimpleDateFormat;import java.util.Date;import java.util.Map;import java.util.Objects;import java.util.Random;import java.util.concurrent.TimeUnit;/** * fastposter云服务客户端 * * @author Alex小新 fastposter@163.com * @site <a href="https://cloud.fastposter.net/"></> */@Builderpublic class FastposterCloudClient {    private static final String CLIENT_TYPE = "java";    private static final String CLIENT_VERSION = "1.0.0";    private static final String USER_AGENT = "fastposter-cloud-client/" + CLIENT_TYPE + ":" + CLIENT_VERSION;    private final SimpleDateFormat SDF = new SimpleDateFormat("yyyy-MM-dd HH:mm:ss.SSS");    private OkHttpClient okHttpClient;    private String endpoint;    private String appKey;    private String appSecret;    private boolean debug;    private boolean trace;//    /**//     * 生成海报//     *//     * @param uuid   海报ID//     * @param params 参数//     * @return//     *///    public Poster buildPoster(String uuid, Map<String, Object> params) {//        return buildPoster(uuid, params, null);//    }////    /**//     * 生成海报//     *//     * @param uuid   海报ID//     * @param params 参数//     * @param type   指定返回的海报图片类型//     * @return//     *///    public Poster buildPoster(String uuid, Map<String, Object> params, PosterType type) {//        return buildPoster(uuid, params, type, null);//    }////    /**//     * 生成海报//     *//     * @param uuid   海报ID//     * @param params 参数//     * @param type   指定返回的海报图片类型//     * @param scale  缩放比例  范围 0.1 ~ 1.0//     * @return//     *///    public Poster buildPoster(String uuid, Map<String, Object> params, PosterType type, double scale) {//        return buildPoster(uuid, params, type, scale, null);//    }////    /**//     * 生成海报//     *//     * @param uuid   海报ID//     * @param params 参数//     * @param type   指定返回的海报图片类型//     * @param b64    是否返回base64//     * @return//     *///    public Poster buildPoster(String uuid, Map<String, Object> params, PosterType type, Boolean b64) {//        return buildPoster(uuid, params, type, null, b64);//    }    /**     * 生成海报     *     * @param uuid   海报ID     * @param params 参数     * @param type   指定返回的海报图片类型     * @param scale  缩放比例  范围 0.1 ~ 1.0     * @param b64    是否返回base64     * @return     */    @SneakyThrows    private <T> T buildPoster(String uuid, Map<String, Object> params, PosterType type, Double scale, Boolean b64, String userAgent, boolean onlySign) {        long time = System.currentTimeMillis();        String data = JsonUtils.toJson(params);        // base64编码        String playload = Base64Utils.encodeToUrlSafeString(data.getBytes());        // 计算签名        Long timestamp = System.currentTimeMillis() / 1000;        String nonce = RandomUtils.randomAlphanumeric(16);        String sign = DigestUtils.md5DigestAsHex((playload + timestamp + nonce + appSecret).getBytes());        // 构建请求参数        GetPosterArgs.GetPosterArgsBuilder build = GetPosterArgs.builder()                .uuid(uuid)                .b64(b64)                .scale(scale)                .timestamp(timestamp)                .nonce(nonce)                .appKey(appKey)                .sign(sign)                .payload(playload);        if (type != null) {            build.type(type.value);        }        GetPosterArgs args = build.build();        String url = this.endpoint + "/v1/build/poster";        MediaType mediaType = MediaType.parse("application/json");        String json = JsonUtils.toJson(args);        if (onlySign) {            return (T) args;        }        if (debug) {            System.out.println(SDF.format(new Date()) + " build poster request=" + json);        }        // 支持用户自己设置userAgent        userAgent = userAgent != null && userAgent.length() > 1 ? userAgent : USER_AGENT;        RequestBody body = RequestBody.create(json, mediaType);        Request request = new Request.Builder().url(url).post(body)                .addHeader("User-Agent", userAgent)                .addHeader("Client-Type", CLIENT_TYPE)                .addHeader("Client-Version", CLIENT_VERSION)                .addHeader("Content-Type", "application/json")                .addHeader("cache-control", "no-cache")                .build();        Response response = okHttpClient().newCall(request).execute();        if (trace) {            response.headers().forEach(a -> System.out.println(a.getFirst() + ":" + a.getSecond()));        }        if (response.code() != 200) {            System.err.println("code=" + response.code());            System.out.println(request);            throw new RuntimeException("build poster error!\n" + response.body().string());        }        String contentType = response.header("content-type");        if ("application/json".equals(contentType)) {            assert response.body() != null;            PosterResponse pr = JsonUtils.parseObject(response.body().bytes(), PosterResponse.class);            if (pr.code != 0) {                throw new RuntimeException("build poster error!\n" + pr.getMsg());            }            return null;        }        assert response.body() != null;        byte[] bytes = response.body().bytes();        String traceId = response.header("fastposter-cloud-traceid");        long size = bytes.length;        if (type == null) {            // 获取服务器返回的图片类型            String rcontentType = response.header("Content-Type");            if (rcontentType != null && !Objects.equals(contentType, "")) {                if (rcontentType.contains("jpeg")) {                    type = PosterType.jpeg;                } else if (rcontentType.contains("pdf")) {                    type = PosterType.pdf;                } else if (rcontentType.contains("png")) {                    type = PosterType.png;                } else if (rcontentType.contains("jpg")) {                    type = PosterType.jpg;                } else {                    type = PosterType.jpg;                }            } else {                type = PosterType.jpg;            }        }        if (debug) {            time = System.currentTimeMillis() - time;            System.out.println(SDF.format(new Date()) + " build poster completed. traceId=" + traceId + " uuid=" + uuid + " type=" + type + " " + (type.value.length() <= 3 ? " " : "") + "size=" + size + " rt=" + time + "ms" + (b64 != null && b64 ? " b64=" + b64 : "") + (scale != null ? " scale=" + scale : ""));        }        return (T) new Poster(traceId, type, bytes, b64);    }    private OkHttpClient okHttpClient() {        if (this.okHttpClient == null) {            this.okHttpClient = new OkHttpClient.Builder().connectTimeout(6, TimeUnit.SECONDS).build();        }        return okHttpClient;    }    public PosterBuilder buildPoster(String uuid) {        return PosterBuilder.builder().client(this).uuid(uuid).build();    }    protected Poster build(PosterBuilder posterBuilder) {        return buildPoster(posterBuilder.uuid, posterBuilder.params, posterBuilder.type, posterBuilder.scale, posterBuilder.b64, posterBuilder.userAgent, false);    }    /**     * 生成签名     *     * @param posterBuilder     * @return     */    protected GetPosterArgs getArgs(PosterBuilder posterBuilder) {        return buildPoster(posterBuilder.uuid, posterBuilder.params, posterBuilder.type, posterBuilder.scale, posterBuilder.b64, posterBuilder.userAgent, true);    }    @Data    @JsonIgnoreProperties(ignoreUnknown = true)    static class PosterResponse {        int code;        String msg;        Object data;        String traceId;    }}